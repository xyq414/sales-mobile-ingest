# 当前状态

最后核验：2026-08-20。

## 结论

`OPPO A6 Pro 5G → Windows MTP → 原始录音 + 公共 CallLog XML → recording/event contract` 已完成真实本机闭环。录音采集、XML artifact、canonical call row 和 event enrichment 都具有持久化去重；原始手机文件保持只读。

云端交付已完成本机首次真实配置和交付：在坚果云客户端公开运行日志证实的 registered SyncRoot 下，已创建专用 `销售通话数据` 目录；当前 canonical 电话已发布为一个完整三文件包。发布、重复发布、常规 MTP ingest 与登录后 watcher 均已验证幂等，不会产生第二个电话包。

## 已验证的真实环境

- 项目：`C:\Users\xieyi\Documents\sales-mobile-ingest`。
- 数据根：`C:\Users\xieyi\Documents\SalesMobileIngestData`；业务代码不依赖固定盘符。
- 当前 Windows MTP：发现 1 台 `OPPO A6 Pro 5G`、1 个 storage、3 个录音候选；录音目录为相对路径 `Music/Recordings/Call Recordings`。
- 当前真实录音：1 个 `.mp3`、570,084 bytes、WPD duration 190.028 秒。手机端仍为只读，重复检查未发现项目造成的删除、移动、改名或修改。
- 当前真实 CallLog export：发现 1 个指定目录与 1 个 `calls-*.xml`；本机 artifact 656 bytes。真实 XML 只在 gitignored diagnostics 中保存。
- 坚果云：Windows 客户端 `7.2.12` 已安装且进程运行。公开客户端运行日志以 `SyncRootManager.Register` 和 `SyncRoot connected` 明确注册 `C:\Users\xieyi\Nutstore\1`；`C:\Users\xieyi\Documents\坚果云演示` 没有对应的注册证据。前者因此是本轮唯一被允许配置的 sync root，不是按目录名、大小或修改时间猜测。
- 本轮显式开发测试身份：`DEV-001 / 开发者测试` 已仅写入 gitignored `config.local.json`。这不是生产销售身份，生产部署必须显式替换，程序不会自行猜测。

## 真机闭环结果

- 录音：首次导入 1，重复运行导入 0 / duplicate 1；ready 保持 1 个媒体和 1 个 recording JSON。
- 公共 XML：首次 artifact 写入 1；后续运行 new artifact 0 / duplicate 1。真实 schema 为 `calls` 根与 `call` 行，parser 产生 1 条 canonical row；后续运行 canonical duplicate 1。
- 关联：唯一候选的时长差 0.028 秒；WPD 时间采用明确的本地时区偏移与录音结束时间对齐，起始差 3.208 秒，状态 `HIGH_CONFIDENCE`。没有伪报 `EXACT`。
- event：当前 ready event 已原子 enrich；号码来源为 `calllog_export`、置信度 `medium`，方向来源为 `calllog_export`。联系人仍未可靠获得。`event_id`、`recording_id`、`media_ref`、媒体 SHA-256、`occurred_at_source=wpd_modified_at` 和 `duration_source=wpd` 均已复核不变。
- 第二次 `ingest-calllog-export --once`：artifact duplicate 1、canonical duplicate 1、`events_already_enriched=1`、`events_enriched=0`，event 文件 hash 不变。
- 后台 watcher 首轮也实际记录了录音 duplicate 1、XML candidate 1、artifact/canonical duplicate 1、`HIGH_CONFIDENCE` 1、无失败；不存在第二份录音、XML artifact 或 event。

## 云端电话包交付层

- 新 contract：`docs/云端电话包接口.md`、`cloud-recording-package/v1`、`cloud-communication-event-package/v1`。
- 结构固定为 `<cloud_handoff_root>/<销售>/<YYYY-MM-DD>/<电话>/`，电话目录严格只有 `audio.<ext>`、`recording.json`、`event.json`。
- 发布采取坚果云同步目录外（`data_root/cloud-handoff-stage`）staging → 完整校验 → 目录最终化；same-volume 使用目录 rename，cross-volume 采用同步目录临时包再 rename。包 validator 同时检查严格三文件、两份 schema、`recording_id`、媒体相对引用和 SHA-256。
- `event_id` 是幂等主键。有效既有包计为 `already_published`；冲突不覆盖；后到 enrich 不生成第二目录，标记 `published_immutable_enrichment_pending`。
- 已配置的专用 handoff root 为已确认 sync root 下的 `销售通话数据`（实际本机绝对路径只存在 gitignored `config.local.json`）。首次 `publish-cloud-handoff --once`：`CLOUD_HANDOFF_READY`、published 1、failures 0。
- 生成一个真实包，目录层级为 `DEV-001_开发者测试/2026-08-18/<event-short>/`。其中严格只有 `audio.mp3`、`recording.json`、`event.json`；CLI validator 通过，两个 JSON schema、媒体 SHA-256、`recording_id`、相对 media ref、日期和销售 identity 都已复核。
- 第二次 publish：published 0、already_published 1、failures 0。随后对同一 OPPO 正常运行 `ingest --once --limit 1`：录音 duplicate 1、event existing 1、cloud already_published 1、无失败；不会新复制电话包。
- 已证明的是“本机 handoff 目录位于当前正在运行的坚果云 registered SyncRoot 内且客户端可见”的交付边界。没有调用坚果云 API、WebDAV、浏览器登录或远端下载校验，因此不宣称远端传播完成。

## 已实现与自动测试

- Windows Shell/MTP 受限目录发现、只读复制、staging、尺寸校验、SHA-256、失败证据保留与本地 state。
- SyncTech v1 provider parser、value-free XML schema summary、canonical call ID、late-arrival state、唯一关联和原子 event enrichment。
- `probe`、`ingest --once`、`watch`、`inspect-calllog-export`、`ingest-calllog-export --once`、`inspect-cloud-handoff`、`configure-salesperson`、`configure-cloud-handoff`、`publish-cloud-handoff --once`、`validate-cloud-package`。
- `pytest`：48 passed，覆盖原有 recording/event/CallLog 链，以及销售身份、Windows 名称清洗、日期/稳定电话目录、严格三文件、相对媒体引用、schema/hash、完整性拒绝、幂等发布、同秒事件、冲突保护、staging 隔离、后到 enrich 不重复发布、cloud contract synthetic example、watcher 单轮自动发布与已确认同步根的专用子目录配置。

## 自动启动

- Task Scheduler 普通用户注册仍被系统拒绝：`ERROR: Access is denied.`（exit code 1）。
- 已使用当前用户 Startup fallback：`C:\Users\xieyi\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\SalesMobileIngest.vbs`。
- 已在配置完成后重新启动并核对：旧的 project watcher 2 个匹配进程均已停止，Startup VBS 启动后出现新的 2 个匹配 parent/child `pythonw` 进程。新进程启动于 2026-08-20 09:53:38 +08:00，09:54:34 +08:00 的新日志为 `CLOUD_HANDOFF_READY`、already_published 1、failures 0。该 watcher 运行当前源码的 `watch --interval 45`，不依赖 Codex。

## GitHub

- private repository：`https://github.com/xyq414/sales-mobile-ingest`，默认分支 `main`。
- 真实录音、XML、客户号码/联系人、state、logs、`.env` 和 `config.local.*` 都被 Git 忽略。
- 采集 feasibility 基线已建立并同步 tag：`v0.1-mvp-feasibility` → `a692f91`。本轮真实 cloud handoff 验证完成后将建立并同步 `v0.2-cloud-handoff`。

## 未验证边界与下一入口

- 已验证的是手动创建的一个 SyncTech CallLog-only XML；计划任务在 OPPO/ColorOS 的长期后台可靠性尚未验证。
- 全量、archive、incremental 多 snapshot、多个手机同时接入、更多 OPPO 样本与其他 exporter 仍需新的真机证据。
- 已有本机 client/root/package/watcher 证据，但没有远端坚果云传播完成证据；远端消费者接入、下载与消费仍是独立后续验证。
- `DEV-001 / 开发者测试` 仅用于本开发机的首次正式配置验证。正式销售机下一步是显式提供真实销售身份，并在各自已确认的 client sync root 下运行相同配置；无需人工复制录音或 XML、无需 ADB。
