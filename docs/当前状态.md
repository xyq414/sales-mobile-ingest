# 当前状态

最后核验：2026-08-31。

## 结论

仓库已从 Recording-first MVP 演进为 Call-first：可靠 CallLog row 会独立发布 `phone-call/v1`，无需录音；录音继续作为独立 `recording-contract/v1`，两者由 `call-recording-link/v1` 保守关联。旧 `communication-event/v1` 和坚果云严格三文件 v1 未改变。

当前又增加了正式 Windows Desktop Pilot：PySide6 首页、首次设备/销售绑定、CallLog/schedule/录音/坚果云就绪检查、worker-thread 一键导入、业务结果、脱敏诊断和 PyInstaller one-folder release。它复用 canonical `Ingestor`，没有第二套 MTP/parser/dedupe/publisher。

本轮 UI/application/release 是 deterministic synthetic + no-phone packaged validation。没有用本 UI 连接真实 OPPO，也没有 Redmi 真机，因此 OPPO Desktop Pilot physical acceptance 为 `NOT_RUN`，Redmi Note 12 5G / Redmi Note 15 仍是 `PHYSICAL_DEVICE_PENDING`；没有任何 synthetic fixture 被标为真机证据。

## 证据分层

### 既有 REAL_DEVICE_VERIFIED（保留）

- 2026-08-19/20 的 OPPO A6 Pro 5G 真实闭环：普通 Windows Shell/MTP、录音只读复制、size/SHA-256、重复跳过、SyncTech CallLog-only XML schema/parser、唯一 `HIGH_CONFIDENCE` 关联、event enrichment、watcher restart 和本机坚果云 registered SyncRoot 三文件 handoff。
- 真实录音/XML、号码、联系人、state、配置和云路径只存在当时 gitignored 本机 data root；本次工作区没有 `config.local.json` 或 runtime state，未重放或修改那些真实数据。
- 上述真实样本只证明 OPPO 既有链路；它不证明本轮新增的多人多机、双卡或 Redmi 物理能力。

### 2026-08-30/31 SYNTHETIC_AUTOMATED / PACKAGED_SMOKE

- 新 canonical contracts：`phone-call/v1`、`call-recording-link/v1`；call-only downstream/cloud 路由不要求 audio。
- missed、rejected、无录音、未知 future type、双 SIM subscription、CallLog-first/Recording-first、NO_MATCH、AMBIGUOUS、full/incremental/archive 重放、多设备相同 call fields、联系人后到和 restart stability。
- Device Registry 使用 local random `device_id`；Shell Path 仅存 hash alias。effective-dated assignment 按 `occurred_at` 归属，overlap 被拒绝。
- state v1/v2 → v3 在迁移前生成私有 backup；唯一历史设备+可证明时间边界可迁移 legacy salesperson，多设备歧义会阻止自动归属。V3 另存跨时间 snapshot 观察和最近 UI import summary。
- snapshot metadata 表达 artifact SHA、backup/import time、count、mode 与 FRESH/STALE/UNKNOWN；malformed XML 保留 failure evidence 且不产生 PhoneCall。
- 新 sync-root 路由 `_phone-call-facts-v1` 与 `_call-recording-links-v1`；旧销售/日期/电话三文件目录继续 immutable。
- Desktop home 的 missing/no XML/FRESH/STALE/UNKNOWN/MALFORMED/count mismatch、schedule evidence、录音 optional、cloud blocker、assignment、double-run、human error 和隐私边界均有 fake/real-adapter 自动断言。
- source GUI smoke 真实创建窗口、5 张卡、向导、设置并截图；packaged smoke 从 repository 外 clean temp 直接运行 windowed EXE 两次，bundled `mtp_bridge.ps1`/schemas、no-phone preflight、配置跨启动持久性和 clean close 均 PASS。
- 正式本地 artifact：`release/SalesMobileIngest-Pilot-win64.zip`（gitignored）；普通用户入口是其中的 `SalesMobileIngest.exe`，不是 Python/PowerShell。

## Pilot human entry

普通销售只需双击 release 中的 `SalesMobileIngest.exe`；第一次填写销售编号/姓名、明确历史边界并确认坚果云同步根，之后解锁手机、选择 MTP、打开程序并点“一键导入到坚果云”。无需 Codex、PowerShell、Python、JSON、XML 路径或手工找录音。

以下 CLI 只保留给开发/验收：

```powershell
python -m sales_mobile_ingest list-devices --discover
python -m sales_mobile_ingest assign-device --device-id "<dev_...>" --salesperson-id "S001" --salesperson-name "张三" --effective-from "2026-01-01T00:00:00+08:00"
python -m sales_mobile_ingest end-device-assignment --device-id "<dev_...>" --effective-to "2026-10-01T00:00:00+08:00"
python -m sales_mobile_ingest show-device --device-id "<dev_...>"
```

未知/变化 alias 会形成未绑定 enrollment；不会按设备显示名、Windows 用户、Git identity、机器名或 legacy 当前销售猜测。历史 XML 即使晚到，也按 call occurred time 查 assignment。

## 自动验收

- `pytest`：117 passed；`HARNESS.md` 将 GC-01 至 GC-30 与 Desktop Pilot matrix 映射到自动断言。
- 旧 recording/event contract、cloud schemas、严格三文件 validator、watcher synthetic cycle 与 cloud v1 compatibility 全部保留在同一 suite。
- 没有 Android 设备时，自动 suite 仍是有效验收；物理 E2E 状态为 NOT_RUN。

## 未完成的物理认证

- OPPO A6 Pro 5G Desktop Pilot：用正式 release 执行首次向导、CallLog preflight、一键导入、结果页、第二次去重和手机源只读检查，当前 `NOT_RUN`。既有 backend REAL_DEVICE_VERIFIED 证据不因此降级，也不能冒充 UI 已验收。

- Redmi Note 12 5G：MTP、真实录音目录/复制/hash/dedupe、真实 XML/双卡/type/correlation、scheduled export 等待更新、watcher restart、手机原文件不变，均待独立执行。
- Redmi Note 15：同一清单必须使用该机自己的 evidence 独立执行，不能继承 Note 12 的结果。
- Xiaomi/HyperOS 继续保持 `OFFICIAL_DOC_CANDIDATE`；只有 `docs/手机初始化与Redmi物理验收.md` 的真实流程通过后才能逐设备改为 `REAL_DEVICE_VERIFIED`。

没有验证或声明 SMS/MMS/RCS、ASR、CRM、ADB production、手机端 agent 或远端坚果云传播完成。
