# 当前状态

最后核验时间：2026-08-19（本机实施轮）。

## 已验证的 Windows 环境事实

- 项目根目录：`C:\Users\xieyi\Documents\sales-mobile-ingest`。
- Python、Git、GitHub CLI 可用；GitHub CLI 当前账号已认证（账号名不在本文件重复记录）。
- 本轮两次只读 PnP/Shell 探测时，Windows Shell 的 `This PC` 仅显示本地/云盘项目，没有 Portable Device；PnP 也没有 `Portable`/`WPD`/OPPO/Android 设备。
- 项目实际 CLI `probe` 正常返回 `devices: []`；`ingest --once` 正常返回 0 个设备、0 个候选、0 个导入、0 个失败。
- 因此该时点的 OPPO A6 Pro 未被本机作为可读 MTP 设备发现，尚无真实录音目录、文件名模式或真机样本证据。不能将合成测试表述为真机 E2E 成功。

## 已实现且已自动测试

- MTP Shell 探针和只读复制桥接；不依赖 ADB 或磁盘盘符。
- OPPO v1 候选判断与 generic guardrail；成功目录写入本机 state 缓存。
- 可配置 `data_root`、原字节暂存/校验/哈希/去重/JSON-last 发布语义。
- `probe`、`ingest --once`、`watch`，以及用户登录后 Task Scheduler 安装/卸载脚本。
- `pytest`：8 项通过，覆盖 schema/example、nullable 字段、稳定 content ID、标准命名、媒体先于 JSON sidecar、内容去重、partial 不进 ready 且后续可恢复、可配置数据根目录、OPPO/generic 候选判断、Music guardrail。
- 真实 CLI 冒烟：PowerShell Shell/MTP bridge 以 Windows PowerShell 5.1 运行，能正确处理“当前无手机”状态；没有把无设备误报为失败或导入成功。

## 自动启动状态

- 尝试创建 Task Scheduler `SalesMobileIngest` 时，系统实际返回 `ERROR: Access is denied.`（exit code 1）。
- 安装脚本已自动使用当前用户 Startup fallback：`C:\Users\xieyi\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\SalesMobileIngest.vbs`。
- 该启动项指向项目 `.venv\Scripts\pythonw.exe` 并运行 `watch --interval 45`，数据根目录为当前用户 Documents 下的 `SalesMobileIngestData`；已读取其 action 文本并实际启动一次。启动测试产生了 1 条无设备 ingest 日志，随后测试 worker 被刻意停止，因此本文件不把它描述为当前持续运行。
- 下次 Windows 用户登录时，此 Startup 项将独立启动观察模式；可运行 `scripts\uninstall-autostart.ps1` 完整移除。

## GitHub 同步状态

- private GitHub 仓库已创建：`https://github.com/xyq414/sales-mobile-ingest`，API 核验为 private，默认分支为 `main`。
- 本地基线 commit：`b868770`；本机 Git HTTPS transport 的首次 push 实际失败，错误为 `Recv failure: Connection was reset`。随后 TCP 探测 GitHub `443` 为失败，因此未进行盲目重试。
- 为保证另一台机器已经能通过 Git 获得当前代码、contract 与状态文档，已使用当前已认证 GitHub CLI 的 Git Database API 发布全部 29 个已追踪文件；远端基线 commit 为 `36a57fb24b5bf164f2ab579daa430b100fc49292`，tree 为 `0b7d297f103a5b42cf7817687d5370efad6e2ab0`。
- 该 API fallback 与本地 root commit 的文件 tree 相同，但 commit 父历史不同（GitHub 需要先建立空仓库 seed commit），所以当前不能把它表述为本机 `git push` 成功。网络恢复后执行正常 fetch/rebase/push 即可收敛提交历史；远端内容已可被另一台机器 clone。

## 待真机验证

手机在本机解锁、USB 选择“媒体文件”且 Windows 可浏览后，运行：

```powershell
.\.venv\Scripts\python.exe -m sales_mobile_ingest probe
.\.venv\Scripts\python.exe -m sales_mobile_ingest ingest --once
.\.venv\Scripts\python.exe -m sales_mobile_ingest ingest --once
```

验收重点是：真实候选目录、至少一个原文件进入 gitignored data root、schema/哈希匹配、第二次不重复，以及手机原文件仍存在。此文档不会保存真实电话、联系人、序列号或录音路径。
