# 当前状态

最后核验时间：2026-08-19（OPPO 真机闭环、通信事件、身份调查，以及 SyncTech 公共 CallLog XML 来源可用性复核）。

## 已验证的 Windows 环境事实

- 项目根目录：`C:\Users\xieyi\Documents\sales-mobile-ingest`。
- Python、Git、GitHub CLI 可用；GitHub CLI 当前账号已认证（账号名不在本文件重复记录）。
- 历史真实验收曾在 Windows Shell / PnP/WPD 发现 `OPPO A6 Pro 5G`，Explorer 可见状态与项目 `probe` 一致；这证明项目不存在已知的“Windows 能看见、项目看不见”枚举缺陷，但不保证手机会一直保持当前可见。
- 真实存储入口为 Windows 显示的内部共享存储空间；已验证的相对目录模式为 `Music/Recordings/Call Recordings`。该 Windows 本地化显示名不进入下游 contract。
- 该目录本轮发现 1 个真实 `.mp3` 候选，大小 570,084 bytes，WPD 报告时长 190.028 秒。本文不保存其原始文件名、号码、联系人或其他客户信息。
- 真机观察到 WPD Shell `Name` 不带扩展名，而 `System.FileName` / `System.FileExtension` 正确给出文件名和 `.mp3`；bridge 已据此修复并增加 regression test。

## SyncTech 公共 CallLog XML 来源（当前复核）

- 用户已在 OPPO 上以正常 App 安装方式配置 SyncTech `SMS Backup & Restore`，并报告创建了一份 CallLog-only 本地 XML 备份；该文件是用户显式创建的导出物，不是通过 ADB、项目 Android APK 或 MTP provider 猜得的通话记录。
- 本次 Windows 复核先运行项目的 Windows Shell/MTP `probe`，随后以 5 秒间隔有限重试 3 次；4 次结果均为 `devices=0`。没有可用 portable device，因此没有枚举用户报告的备份目录、没有读取或复制 XML、没有输出 XML 值、没有得到真实根节点、字段名、字段类型、内容 hash 或 canonical row。
- 终态为 `WINDOWS_MTP_DEVICE_NOT_CURRENTLY_AVAILABLE`。这是当前 Windows 物理设备可见性结论，不是 XML 不存在、SyncTech 备份失败或其 CallLog 内容为空的结论。本轮严格没有回退到 ADB。
- 因为实际 XML schema 尚未读到，尚未实现 parser、canonical call ID、去重 state、recording correlation 或 event enrichment；现有真实 recording/event 均未被修改。对应的设计草案与可验证边界在 [通话记录导出接口.md](通话记录导出接口.md)。
- 原始 XML 未来只允许落在 gitignored 的 `data_root/diagnostics/calllog-backup`；它绝不进入 Git、README、contract example、常规日志或下游 `ready`。目前该目录没有由本项目在本轮写入的 XML。

## 已实现且已自动测试

- MTP Shell 探针和只读复制桥接；不依赖 ADB 或磁盘盘符。
- OPPO v1 候选判断与 generic guardrail；成功目录写入本机 state 缓存。
- 可配置 `data_root`、原字节暂存/校验/哈希/去重/JSON-last 发布语义。
- `probe`、`ingest --once`、`watch`，以及用户登录后 Task Scheduler 安装/卸载脚本。
- `pytest`：当前 28 项，覆盖原有 recording/event contract、MTP/WPD 与 identity regression，以及 Android CallLog probe 的最小 Manifest、固定 package、无 `INTERNET`、有界 app-private result parser、号码掩码和 recording ↔ CallLog 合成关联判定。
- 新增 `ingest --once --limit N`：只限制某次命令的源文件复制尝试，首次真机验证可有界运行；长期正常增量行为仍默认不限量。
- `communication_event.schema.json`、中文接口说明和合成 phone-call example 已加入。每个 ready recording pair 会自动、幂等地发布一个 `ready/events/<event_id>.json`，并且 event 仅在媒体与录音 sidecar 已完成后出现。
- event `media_ref` 永远是相对 `data_root` 的 `ready/recordings/...` 路径；`event_id` 由 source type 和 `recording_id` 可复算。号码不做文件名猜测或国家码推断；本机未配置销售身份时明确输出 `salesperson_identity_status=UNCONFIGURED`。
- 新增 `probe --save-report`，本机报告写入 gitignored 的 `diagnostics/probe-reports`；只记录目录、扩展、数量、尺寸、WPD 元数据可用性、脱敏文件名结构，不保存原始文件名、号码、联系人、设备序列号或音频。
- 厂商状态有机器可读 profile，并同步到 [厂商适配矩阵.md](厂商适配矩阵.md)：OPPO 为 `REAL_DEVICE_VERIFIED`；Xiaomi、Huawei、Honor 是官方资料路径候选而非真机支持；vivo 为 `DOC_EVIDENCE_UNAVAILABLE`，未猜测路径；generic 只在明确通话目录证据下工作。

## 真机闭环结果

- `probe`：发现 1 台 OPPO、3 个候选目录，其中 `Music/Recordings/Call Recordings` 以目录语义+实际音频证据被 `oppo-v1` 接受。
- `ingest --once --limit 1`：1 次复制尝试，导入 1，重复 0，失败 0；仅复制本轮一个 smoke sample，没有批量迁移历史。
- ready 位置：`C:\Users\xieyi\Documents\SalesMobileIngestData\ready\recordings`，其中已有 1 个原始媒体和 1 个 JSON sidecar。
- 真机 sidecar 已通过 `recording.schema.json`；其 `sha256` 与本地媒体重新计算一致，`recording_id` 可由 SHA-256 重新计算，`recorded_at_source` 为 `source_modified_at`，时长为 190.028 秒；号码/联系人为 `null`，方向为 `unknown`。
- 同一手机源再次 `ingest --once --limit 1`：导入 0，重复 1，失败 0；ready 仍为 1 个 media + 1 个 JSON，state 已持久化。
- 导入前后重新枚举手机：源目录仍为 1 个文件、总大小仍为 570,084 bytes，未发生手机端删除、移动、改名或修改。
- 手工 `watch --interval 10` 冒烟：真实发现 OPPO 且记录了重复跳过；测试 worker 已停止，没有残留手工 watcher。
- 本轮新版 `probe --save-report` 真实发现 1 台 OPPO、3 个限定候选；`Music/Recordings/Call Recordings` 仍是唯一 accepted 候选。报告已写入本机 gitignored diagnostics 目录，未进入仓库。
- 本轮新版 `ingest --once --limit 1`：导入 0、重复 1、失败 0，并从既有完整 recording pair 自动发布 1 个真实 `phone_call` event。再次执行后：导入 0、重复 1、事件新建 0、事件已存在 1、事件失败 0。
- 最终代码版本再次连续执行两次 `ingest --once --limit 1`：两次均为 devices=1、accepted candidates=1、导入 0、重复 1、失败 0、事件新建 0、事件已存在 1、事件失败 0；不会产生第二个 recording 或第二个 event。
- 已在本机重新校验：ready recordings=1、ready events=1；event schema 与 recording schema 都有效，`media_ref` 是相对路径，event 的 `recording_id` 与 recording 一致，媒体 SHA-256 与 event/recording 均一致；当前真实 WPD 时长 metadata 可用，legacy sidecar 已以相同 source relative path 和相同数值补记 `duration_source=wpd`。
- 本轮后再次 MTP 枚举手机源目录：仍为 1 个文件、总大小 570,084 bytes；本仓库始终只读手机端。
- 本轮完成当前唯一真实 recording 的身份调查：文件名保守号码候选=0、WPD 身份属性号码候选=0、MP3 ID3 tag 号码候选=0；结论为 `DIRECT_RECORDING_PHONE_ID_NOT_FOUND_IN_CURRENT_SAMPLE`。WPD 有 modified time、duration 和媒体属性，但没有 created time，且没有可可靠填入的联系人或呼入/呼出字段。
- 同时完成当前普通 MTP/WPD 通话记录能力调查：1 个 storage、14 个首层公开对象，没有 call-history、contacts 或 backup/export object；结论为 `CALL_LOG_NOT_EXPOSED_VIA_CURRENT_MTP_WPD`。这不是“手机没有通话记录”，而是当前无侵入 USB 文件传输链路无法读取该 provider；详见 [通话身份解析能力.md](通话身份解析能力.md)。

## 历史 Android CallLog companion feasibility probe（非当前优先路径）

- 新增隔离目录 `android/calllog-probe`：最小 debug Android App，package=`com.salesmobileingest.calllogprobe`，只声明 `READ_CALL_LOG`，不声明 `INTERNET`、写 call log、联系人、存储、默认拨号器、后台服务或任何网络能力。它未接入 production ingest、ready event 或 Windows watcher。
- 本机原来没有 Java、Android SDK 或 ADB；本轮在 gitignored 的 `android/.tooling` 自动 bootstrap JDK 17、Android SDK Platform 36、Build-Tools 36.0.0、Platform-Tools/ADB 及 Gradle 构建工具。`assembleDebug` 真实成功，编译 APK 以 `aapt` 静态确认 package 正确且只含 `android.permission.READ_CALL_LOG`。
- 真实 ADB 检查：`adb devices -l` 只返回 `List of devices attached` 标题行；当前 device state=`NONE`。收尾复核时，MTP `ingest --once --limit 1` 同样返回 `devices=0`，因此没能再次枚举手机源；先前已验证的 MTP 闭环仍是历史事实，但当前物理设备可见性为 absent。这不是自动 APK 安装协议。因此真实终态是 `AUTOMATED_ANDROID_APP_INSTALL_UNAVAILABLE_IN_CURRENT_DEVICE_STATE`，不是 `READ_CALL_LOG` 的 OPPO 拒绝结论。
- APK 从未安装到 OPPO；因此 Manifest declaration=`YES`，但 runtime grant、permission flags、appops、CallLog provider query、真实号码/方向/联系人和 190.028 秒录音匹配均为 `BLOCKED`。没有伪造 `CALL_LOG_PROBE_SUCCESS`。
- probe raw evidence 设计为 app-private `files/probe-result.json`，Windows 侧仅在未来真实运行时保存到 gitignored 的 `diagnostics/android-calllog-probe`；本轮只有安全的 build/ADB status，无电话行、号码、序列号或 app-private payload 进入仓库。
- 手机 probe APK cleanup=`NEVER_INSTALLED`；正式 event hash、ready media 数量和字节数均在本轮前后相同；Windows watcher 未被本轮 Android 工作修改。因收尾时 MTP device=`0`，手机原文件的最后一次直接枚举状态为 `NOT_RECHECKED_AT_CLOSE_DEVICE_UNAVAILABLE`，但本轮没有手机写路径或已安装 APK。完整能力与官方 permission 边界见 [通话身份解析能力.md](通话身份解析能力.md)。
- 用户后续确认已开启 OPPO 开发者选项与 USB debugging 后，本机立即重新执行 ADB：直接检查和 Windows ADB server 重启后的 3 次有限重试均为 `ADB_DEVICE_NONE`，没有 `device`、`unauthorized` 或 `offline`。非敏感 PnP 摘要和项目 MTP `probe` 同时为 0 个当前设备。这个结果只说明当前 Windows 尚未枚举到调试设备，不能判断手机设置是否正确，更不能判断 `READ_CALL_LOG` 被拒绝；本轮没有发生 APK 安装、permission/appops/provider 调用或手机文件写操作。APK 重建通过，`pytest` 仍为 `28 passed`。

## 自动启动状态

- 尝试创建 Task Scheduler `SalesMobileIngest` 时，系统实际返回 `ERROR: Access is denied.`（exit code 1）。
- 安装脚本已自动使用当前用户 Startup fallback：`C:\Users\xieyi\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\SalesMobileIngest.vbs`。
- 该启动项仍指向项目 `.venv\Scripts\pythonw.exe` 并运行 `watch --interval 45`，数据根目录为当前用户 Documents 下的 `SalesMobileIngestData`；本轮手工 `watch --interval 10` 测试 worker 已停止。
- 本轮在更新代码后，只停止了项目自身旧 watcher，并通过同一 Startup VBS 重新启动。启动 launcher 已退出；当前有一个逻辑 watcher（一个 launcher/一个 child 的两个 `pythonw` 进程），均在最新源码修改后创建，且其后的日志已更新。这不是 Codex 工具进程持有的临时 worker。
- `scripts\uninstall-autostart.ps1` 现会同时移除下次登录注册并停止匹配的当前 watcher；无需重启即可卸载。

## GitHub 同步状态

- private GitHub 仓库已创建：`https://github.com/xyq414/sales-mobile-ingest`，API 核验为 private，默认分支为 `main`。
- 初始网络异常期间曾使用 GitHub Git Database API 发布基线内容；本轮普通 Git transport 已恢复。
- 已 `fetch` 并验证 API 发布的远端 tree 与本地已发布 tree 完全一致，随后用普通 Git merge 连接两段历史并成功 `git push origin main`；未使用 force push。
- 当前远端 `main` 已包含 OPPO WPD 修复、真机验收状态、测试与文档，另一台机器可通过普通 Git clone/pull 获取；Android probe source、测试、文档和忽略规则已在本轮以普通 Git push 同步。

## 后续验证边界

本轮已完成第一条真实 OPPO 录音闭环。当前号码/方向等身份字段的下一条优先验证路径是用户显式导出的公共 CallLog XML，而非继续调试 ADB companion。Windows 重新以 MTP 发现手机后，先只读取得实际 XML schema 摘要，再实施并验收解析、去重、关联及原子 event enrichment。未来新增机型、更多 OPPO 文件名规律、全量历史导入策略和设备断连恢复仍需以新的真实证据扩展；不得把本轮 1 个样本的私有文件名或客户信息写入规则或仓库。
