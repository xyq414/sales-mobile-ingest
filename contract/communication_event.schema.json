{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sales-mobile-ingest.local/contract/communication_event.schema.json",
  "title": "Sales Mobile Ingest Communication Event Contract v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "event_id", "source_type", "recording_id", "occurred_at", "occurred_at_source",
    "occurred_at_confidence", "salesperson_id", "salesperson_identity_status", "installation_id", "device_vendor",
    "device_model", "phone_number_raw", "phone_number_normalized", "phone_number_source", "phone_number_confidence",
    "contact_name", "contact_name_source", "call_direction", "call_direction_source", "duration_seconds",
    "duration_source", "media_ref", "media_sha256", "original_extension", "ingested_at", "adapter",
    "adapter_evidence_status"
  ],
  "properties": {
    "schema_version": {"const": "communication-event/v1"},
    "event_id": {"type": "string", "pattern": "^evt_[a-f0-9]{64}$"},
    "source_type": {"type": "string", "minLength": 1},
    "recording_id": {"type": ["string", "null"], "pattern": "^rec_[a-f0-9]{64}$"},
    "occurred_at": {"type": ["string", "null"], "format": "date-time"},
    "occurred_at_source": {"enum": ["filename", "wpd_modified_at", "wpd_created_at", "import_time", "unknown"]},
    "occurred_at_confidence": {"enum": ["high", "medium", "low", "unknown"]},
    "salesperson_id": {"type": ["string", "null"]},
    "salesperson_identity_status": {"enum": ["CONFIGURED", "UNCONFIGURED"]},
    "installation_id": {"type": "string", "pattern": "^ins_[0-9a-f-]{36}$"},
    "device_vendor": {"type": ["string", "null"]},
    "device_model": {"type": ["string", "null"]},
    "phone_number_raw": {"type": ["string", "null"]},
    "phone_number_normalized": {"type": ["string", "null"]},
    "phone_number_source": {"enum": ["filename", "wpd_metadata", "audio_metadata", "manual", "unknown"]},
    "phone_number_confidence": {"enum": ["high", "medium", "low", "unknown"]},
    "contact_name": {"type": ["string", "null"]},
    "contact_name_source": {"enum": ["filename", "wpd_metadata", "audio_metadata", "manual", "unknown"]},
    "call_direction": {"enum": ["incoming", "outgoing", "unknown", null]},
    "call_direction_source": {"enum": ["filename", "wpd_metadata", "manual", "unknown"]},
    "duration_seconds": {"type": ["number", "integer", "null"], "minimum": 0},
    "duration_source": {"enum": ["wpd", "media_probe", "unknown"]},
    "media_ref": {"type": "string", "pattern": "^ready/recordings/[^\\\\/]+\\.[A-Za-z0-9]{1,10}$"},
    "media_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "original_extension": {"type": "string", "pattern": "^\\.[A-Za-z0-9]{1,10}$"},
    "ingested_at": {"type": "string", "format": "date-time"},
    "adapter": {"type": "string", "minLength": 1},
    "adapter_evidence_status": {"enum": ["REAL_DEVICE_VERIFIED", "OFFICIAL_DOC_CANDIDATE", "DOC_EVIDENCE_UNAVAILABLE", "GENERIC_HEURISTIC"]}
  }
}
