# Workflow source

This repository inherits the user's local global Codex workflow. Its project-local overlay is `AGENTS.md`.

For this project, the important acceptance boundary is a real Windows MTP probe followed by a read-only ingest into a gitignored data root. Synthetic fixtures may validate code paths but do not prove a physical phone is available.
